# TEMPORARY probe round 4 — confirm the exact production shape.
# Round 3 established: _redirects works, sources AND targets carry the mount
# prefix, and a 200-proxy fires — but a target ending in .html then eats the
# worker's extension-strip 307 (so .../index.html becomes .../ and loops).
# Targets must therefore be clean, extensionless URLs.

/d13-app/_canary2   /home   301

# the real shape: folder page named main.html, proxied from the short URL
/d13-app/_probe-d   /d13-app/_probe-d/main   200

# same idea against the existing nested probe
/d13-app/_probe-b   /d13-app/_probe-b/inner   200
