# TEMPORARY probe round 3.
# Established: _redirects IS honored, and rule SOURCES must carry the mount
# prefix (/d13-app/_canary2 -> /home 301 fired; the bare /_canary did not).
# Round 2 also showed a prefixed source with a BARE target 404s rather than
# looping — the rule matched but the target did not resolve. So this round
# varies only the target.

# canary stays, proves the file is live each round
/d13-app/_canary2   /home   301

# a: target prefixed, pointing at a directory index
/d13-app/_probe-a   /d13-app/_probe-a/index.html   200

# b: target prefixed, pointing at a NON-index file (is index.html the problem?)
/d13-app/_probe-b   /d13-app/_probe-b/inner.html   200

# c: target is an existing clean URL rather than a file path
/d13-app/_probe-c   /d13-app/home   200
