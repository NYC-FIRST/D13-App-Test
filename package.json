{
  "name": "d13-webflow-wrapper",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Astro wrapper that packages the repo's static site for Webflow Cloud. Adds no code to the site itself.",
  "scripts": {
    "build": "astro build",
    "dev": "astro dev",
    "test": "node check.mjs"
  },
  "devDependencies": {
    "astro": "^7.3.2"
  }
}
